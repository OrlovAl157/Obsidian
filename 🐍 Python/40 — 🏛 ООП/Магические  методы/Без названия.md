a + b  →  a.__add__(b)
          ↓
       NotImplemented?
          ↓
    тип(a) == тип(b)?
    ДА → TypeError сразу
    НЕТ → b.__radd__(a)
              ↓
           NotImplemented?
              ↓
           TypeError