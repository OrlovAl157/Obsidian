---
tags:
  - python
  - тема/ооп
  - тема/дескрипторы
  - тема/магические_методы
  - статус/справка
уровень: продвинутый
---

# property vs __get__ vs __getattr__: когда вызывается

```
====================================================
        DESCRIPTOR PROTOCOL (дескрипторы)
====================================================

Используется когда атрибут класса является дескриптором:
property, cached_property, ORM-поля, validators и т.д.

class A:
    x = Descriptor()

----------------------------------------------------

__set_name__(self, owner, name)
    ↓
Вызывается ОДИН РАЗ при создании класса.
Сообщает дескриптору:
- в каком классе он находится
- под каким именем сохранён

Пример:
    x = Descriptor()
             ↓
    name == 'x'

----------------------------------------------------

__get__(self, instance, owner)
    ↓
Вызывается при:
    obj.attr

Отвечает за получение значения атрибута.

----------------------------------------------------

__set__(self, instance, value)
    ↓
Вызывается при:
    obj.attr = value

Отвечает за присваивание значения.

----------------------------------------------------

__delete__(self, instance)
    ↓
Вызывается при:
    del obj.attr

Отвечает за удаление значения.


====================================================
        МАГИЧЕСКИЕ МЕТОДЫ ОБЪЕКТА
====================================================

Работают почти при любом обращении к атрибутам.

----------------------------------------------------

__getattribute__(self, name)
    ↓
Вызывается ВСЕГДА при:
    obj.attr

Главный обработчик доступа к атрибутам.

----------------------------------------------------

__getattr__(self, name)
    ↓
Вызывается ТОЛЬКО если атрибут не найден.

Работает после __getattribute__.

----------------------------------------------------

__setattr__(self, name, value)
    ↓
Вызывается при:
    obj.attr = value

Перехватывает любое присваивание атрибута.

----------------------------------------------------

__delattr__(self, name)
    ↓
Вызывается при:
    del obj.attr

Перехватывает удаление атрибута.


====================================================
                PROPERTY
====================================================

property — это data descriptor.

@property
    ↓
создаёт getter
(использует __get__)

----------------------------------------------------

@x.setter
    ↓
создаёт setter
(использует __set__)

----------------------------------------------------

@x.deleter
    ↓
создаёт deleter
(использует __delete__)


====================================================
             ВАЖНОЕ РАЗЛИЧИЕ
====================================================

Descriptor protocol
    ↓
управляет АТРИБУТОМ КЛАССА

__getattribute__ / __setattr__
    ↓
управляют самим ОБЪЕКТОМ
```

---

## Связанные темы

- [[05 — 🏠 Свойства property() и @property]]
- [[Атрибуты объекта getter setter]]
- [[06 — 🔧 Типы методов @classmethod и @staticmethod]]
- [[03 — 🔍 Протокол дескрипторов]]

---

#python/descriptors #oop #protocols #магические_методы #справка
