---
tags: [sql, функции, if, case, coalesce]
---

# 📝 SQL дополнительные функции MySQL — памятка

> Краткая шпаргалка по условным и служебным функциям MySQL: работа с `NULL`, выбор значений, приведение типов и конструкция `CASE`.

## Содержание

- [[#Справка по функциям|Справка по функциям]]
- [[#Работа с null|Работа с NULL]]
- [[#Условные функции|Условные функции]]
- [[#Приведение типов|Приведение типов]]
- [[#Конструкция case|Конструкция CASE]]

## Справка по функциям

| Функция | Что делает | Синтаксис |
| --- | --- | --- |
| `ISNULL` | Проверяет, равно ли значение `NULL` | `ISNULL(expr)` |
| `COALESCE` | Возвращает первое непустое значение | `COALESCE(a, b, ...)` |
| `IFNULL` | Возвращает запасное значение вместо `NULL` | `IFNULL(expr1, expr2)` |
| `NULLIF` | Сравнивает два значения и может вернуть `NULL` | `NULLIF(a, b)` |
| `IF` | Возвращает одно из двух значений по условию | `IF(cond, a, b)` |
| `CONVERT` | Приводит значение к типу | `CONVERT(expr, type)` |
| `CAST` | Явно преобразует значение к типу | `CAST(expr AS type)` |
| `CASE` | Позволяет задать несколько условий | `CASE ... END` |

## Работа с NULL

**Назначение:** проверять `NULL`, подставлять значения по умолчанию и выбирать первое непустое значение.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `ISNULL` | `ISNULL(expr)` | Проверяет, является ли значение `NULL`. |
| `COALESCE` | `COALESCE(a, b, ...)` | Возвращает первое не-`NULL` значение. |
| `IFNULL` | `IFNULL(expr1, expr2)` | Если первое значение `NULL`, возвращает второе. |
| `NULLIF` | `NULLIF(a, b)` | Если значения равны, возвращает `NULL`. |

**Примеры:**

```sql
SELECT ISNULL(NULL);
-- 1

SELECT COALESCE(NULL, NULL, 2, 3);
-- 2

SELECT IFNULL(NULL, 'geek');
-- geek

SELECT NULLIF('bee', 'bee');
-- NULL

SELECT NULLIF('bee', 'geek');
-- bee
```

**Важное:** `COALESCE()` особенно полезна, когда нужно перебрать сразу несколько возможных источников значения.

## Условные функции

**Назначение:** возвращать разные результаты в зависимости от условия.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `IF` | `IF(cond, a, b)` | Возвращает одно из двух значений по условию. |

**Примеры:**

```sql
SELECT IF(1 < 0, 'bee', 'geek');
-- geek
```

**Важное:** `IF()` удобна для коротких условий, когда не нужен полный `CASE`.

## Приведение типов

**Назначение:** явно преобразовывать значение к нужному типу.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `CONVERT` | `CONVERT(expr, type)` | Преобразует значение к типу в стиле MySQL. |
| `CAST` | `CAST(expr AS type)` | Явное преобразование в SQL-стиле. |

**Примеры:**

```sql
SELECT CONVERT(100.78, DECIMAL(4, 1));
-- 100.8

SELECT CAST(100.78 AS DECIMAL(3, 0));
-- 101
```

**Важное:** `CAST()` и `CONVERT()` похожи по назначению, но синтаксис у них разный.

## Конструкция CASE

**Назначение:** описать несколько условий и выбрать результат по одному из них.

| Вариант | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `CASE expr` | `CASE expr WHEN ... THEN ... END` | Удобен, когда сравнивается одно выражение с разными значениями. |
| `CASE` | `CASE WHEN cond THEN ... END` | Удобен для произвольных логических условий. |

**Примеры:**

Вариант 1: сравнение с выражением

```sql
CASE quantity
    WHEN 0 THEN 'Out of stock'
    WHEN 1 THEN 'One in stock'
    WHEN 2 THEN 'Little in stock'
    WHEN 3 THEN 'Little in stock'
    WHEN 4 THEN 'Little in stock'
    WHEN 5 THEN 'Little in stock'
    ELSE 'A lot in stock'
END AS availability
```

Вариант 2: логические условия

```sql
CASE
    WHEN quantity = 0 THEN 'Out of stock'
    WHEN quantity = 1 THEN 'One in stock'
    WHEN quantity BETWEEN 2 AND 5 THEN 'Little in stock'
    ELSE 'A lot in stock'
END AS availability
```

Результат выполнения

```text
+--------------------+
| availability       |
+--------------------+
| A lot in stock     |
| One in stock       |
| Little in stock    |
| Little in stock    |
| Out of stock       |
+--------------------+
```

**Важное:** `CASE` удобнее `IF()`, когда условий больше двух или когда логика должна оставаться читаемой.
