# 📝 SQL текстовые функции MySQL — памятка

> [!summary] Что это
> Быстрая шпаргалка по текстовым функциям MySQL: что делает функция, как выглядит синтаксис и где ее удобнее всего применять.

## 📋 Справка по функциям

| Функция | Что делает | Синтаксис | Раздел |
| --- | --- | --- | --- |
| `CONCAT` | Соединяет строки | `CONCAT(str1, str2, ...)` | [📦 Соединение](#join) |
| `CONCAT_WS` | Соединяет строки через разделитель | `CONCAT_WS(sep, str1, str2, ...)` | [📦 Соединение](#join) |
| `GROUP_CONCAT` | Собирает значения из нескольких строк в одну | `GROUP_CONCAT(expr SEPARATOR ', ')` | [📦 Соединение](#join) |
| `CHAR_LENGTH` | Возвращает длину в символах | `CHAR_LENGTH(str)` | [🔍 Поиск и длина](#search-length) |
| `LOCATE` | Ищет позицию подстроки | `LOCATE(substr, str[, start])` | [🔍 Поиск и длина](#search-length) |
| `LEFT` | Берет первые `n` символов | `LEFT(str, n)` | [✂️ Извлечение](#extract) |
| `RIGHT` | Берет последние `n` символов | `RIGHT(str, n)` | [✂️ Извлечение](#extract) |
| `SUBSTRING` | Возвращает подстроку по позиции | `SUBSTRING(str, start[, len])` | [✂️ Извлечение](#extract) |
| `SUBSTRING_INDEX` | Возвращает часть строки по разделителю | `SUBSTRING_INDEX(str, delim, count)` | [✂️ Извлечение](#extract) |
| `LOWER` | Переводит в нижний регистр | `LOWER(str)` | [🔤 Регистр](#case) |
| `UPPER` | Переводит в верхний регистр | `UPPER(str)` | [🔤 Регистр](#case) |
| `ORD` | Возвращает код первого символа | `ORD(str)` | [🔢 Коды символов](#codes) |
| `CHAR` | Собирает символы по кодам | `CHAR(code1, code2, ...)` | [🔢 Коды символов](#codes) |
| `LPAD` | Дополняет строку слева | `LPAD(str, len, pad)` | [📏 Выравнивание](#padding) |
| `RPAD` | Дополняет строку справа | `RPAD(str, len, pad)` | [📏 Выравнивание](#padding) |
| `LTRIM` | Удаляет пробелы слева | `LTRIM(str)` | [🧹 Очистка краев](#trim) |
| `RTRIM` | Удаляет пробелы справа | `RTRIM(str)` | [🧹 Очистка краев](#trim) |
| `TRIM` | Удаляет пробелы или указанные символы по краям | `TRIM([LEADING \| TRAILING \| BOTH] [remstr FROM] str)` | [🧹 Очистка краев](#trim) |
| `REPLACE` | Заменяет фрагменты строки | `REPLACE(str, from, to)` | [🔄 Модификация](#modify) |
| `REVERSE` | Переворачивает строку | `REVERSE(str)` | [🔄 Модификация](#modify) |
| `REPEAT` | Повторяет строку `n` раз | `REPEAT(str, n)` | [🔄 Модификация](#modify) |

---

<a id="join"></a>

## 📦 Соединение строк

> [!tip] Когда использовать
> Когда нужно собрать имя, адрес, путь, список значений или агрегировать текст из нескольких строк.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `CONCAT` | `CONCAT(str1, str2, ...)` | Если хотя бы один аргумент `NULL`, результат тоже `NULL`. |
| `CONCAT_WS` | `CONCAT_WS(separator, str1, str2, ...)` | Пропускает `NULL` и не вставляет для них разделитель. |
| `GROUP_CONCAT` | `GROUP_CONCAT(expr [ORDER BY ...] [SEPARATOR str])` | Агрегатная функция: собирает значения из нескольких строк результата. |

**Примеры**

```sql
SELECT CONCAT('John', ' ', 'Doe');
-- John Doe

SELECT CONCAT_WS('-', 'USA', 'NY', 'Manhattan');
-- USA-NY-Manhattan

SELECT CONCAT_WS(', ', 'Apple', NULL, 'Banana');
-- Apple, Banana

SELECT GROUP_CONCAT(name SEPARATOR ', ')
FROM users;
-- Peter, Gwen, Flash, Mary

SELECT GROUP_CONCAT(name ORDER BY name SEPARATOR ' | ')
FROM users;
```

> [!warning] Важно
> `GROUP_CONCAT` часто используют с `GROUP BY`, но не только. Еще у него есть лимит длины результата: `group_concat_max_len`.

---

<a id="search-length"></a>

## 🔍 Поиск подстроки и длина

> [!tip] Когда использовать
> Когда нужно найти позицию символов, проверить наличие фрагмента или посчитать длину строки.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `LOCATE` | `LOCATE(substr, str[, start])` | Возвращает позицию первого вхождения, нумерация идет с `1`. Если не найдено, вернет `0`. |
| `CHAR_LENGTH` | `CHAR_LENGTH(str)` | Возвращает количество символов, а не байтов. |

**Примеры**

```sql
SELECT CHAR_LENGTH('beegeek');
-- 7

SELECT LOCATE('geek', 'beegeek');
-- 4

SELECT LOCATE('e', 'beegeek', 3);
-- 4

SELECT LOCATE('xyz', 'beegeek');
-- 0
```

> [!info] Нюанс
> Чувствительность `LOCATE` к регистру зависит от `collation`. Если нужна длина в байтах, а не в символах, смотри `LENGTH()`.

---

<a id="extract"></a>

## ✂️ Извлечение части строки

> [!tip] Когда использовать
> Когда надо взять начало строки, конец, подстроку по позиции или часть строки до разделителя.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `LEFT` | `LEFT(str, n)` | Возвращает первые `n` символов. |
| `RIGHT` | `RIGHT(str, n)` | Возвращает последние `n` символов. |
| `SUBSTRING` | `SUBSTRING(str, start[, len])` | Поддерживает отрицательный `start`, тогда отсчет идет с конца строки. |
| `SUBSTRING_INDEX` | `SUBSTRING_INDEX(str, delim, count)` | Работает по разделителю, а не по числовой позиции. |

**Примеры**

```sql
SELECT LEFT('beegeek', 3);
-- bee

SELECT RIGHT('beegeek', 3);
-- eek

SELECT SUBSTRING('beegeek', 4, 2);
-- ge

SELECT SUBSTRING('beegeek', -4);
-- geek

SELECT SUBSTRING_INDEX('g-e-e-k', '-', 2);
-- g-e

SELECT SUBSTRING_INDEX('g-e-e-k', '-', -1);
-- k
```

> [!info] Нюанс
> `SUBSTRING()` удобнее для работы с позициями, а `SUBSTRING_INDEX()` для email, доменов, путей и строк вида `a/b/c`.

---

<a id="case"></a>

## 🔤 Изменение регистра

> [!tip] Когда использовать
> Когда нужно нормализовать текст и привести строки к одному виду перед сравнением или выводом.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `LOWER` | `LOWER(str)` | Переводит буквы в нижний регистр. |
| `UPPER` | `UPPER(str)` | Переводит буквы в верхний регистр. |

**Примеры**

```sql
SELECT LOWER('BeeGeek SQL');
-- beegeek sql

SELECT UPPER('BeeGeek SQL');
-- BEEGEEK SQL

SELECT LOWER(NULL);
-- NULL
```

> [!warning] Важно
> Выражения вроде `LOWER(name)` удобны для обучения и нормализации, но в фильтрах могут мешать использованию индекса по столбцу.

---

<a id="codes"></a>

## 🔢 Коды символов и `CHAR`

> [!tip] Когда использовать
> Когда нужно получить числовой код символа или собрать строку по кодам.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `ORD` | `ORD(str)` | Возвращает числовой код первого символа. |
| `CHAR` | `CHAR(code1, code2, ...)` | Возвращает строку, собранную из переданных кодов. |

**Примеры**

```sql
SELECT ORD('A');
-- 65

SELECT ORD('Z');
-- 90

SELECT CHAR(65, 66, 67);
-- ABC
```

> [!info] Нюанс
> В учебных примерах это часто называют “ASCII”, но на практике поведение связано еще и с кодировкой/`charset`.

---

<a id="padding"></a>

## 📏 Выравнивание и дополнение

> [!tip] Когда использовать
> Когда строку нужно довести до фиксированной длины для форматированного вывода.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `LPAD` | `LPAD(str, len, pad)` | Добавляет символы слева до длины `len`. |
| `RPAD` | `RPAD(str, len, pad)` | Добавляет символы справа до длины `len`. |

**Примеры**

```sql
SELECT LPAD('bee', 6, '-');
-- ---bee

SELECT RPAD('bee', 6, '-');
-- bee---

SELECT LPAD('bee', 6, '-+-');
-- -+-bee

SELECT LPAD('verylong', 3, '-');
-- ver
```

> [!warning] Важно
> Если `len` меньше длины исходной строки, MySQL не дополняет, а обрезает результат.

---

<a id="trim"></a>

## 🧹 Удаление пробелов и символов по краям

> [!tip] Когда использовать
> Когда нужно очистить строку от пробелов или одинаковых символов в начале и конце.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `LTRIM` | `LTRIM(str)` | Удаляет пробелы только слева. |
| `RTRIM` | `RTRIM(str)` | Удаляет пробелы только справа. |
| `TRIM` | `TRIM([LEADING \| TRAILING \| BOTH] [remstr FROM] str)` | Удаляет пробелы по умолчанию или указанный фрагмент с края строки. |

**Примеры**

```sql
SELECT LTRIM('   bee');
-- bee

SELECT RTRIM('bee   ');
-- bee

SELECT TRIM('   bee   ');
-- bee

SELECT TRIM(LEADING '-' FROM '--bee--');
-- bee--

SELECT TRIM(TRAILING '-' FROM '--bee--');
-- --bee

SELECT TRIM(BOTH '-' FROM '--bee--');
-- bee
```

> [!warning] Важно
> `TRIM` не заменяет `REPLACE`. Он работает только с краями строки, а не с серединой.

---

<a id="modify"></a>

## 🔄 Преобразование и модификация

> [!tip] Когда использовать
> Когда нужно заменить фрагмент текста, развернуть строку или получить повторяющийся шаблон.

| Функция | Синтаксис | Что важно помнить |
| --- | --- | --- |
| `REPLACE` | `REPLACE(str, from, to)` | Меняет все вхождения `from` на `to`. |
| `REVERSE` | `REVERSE(str)` | Разворачивает строку задом наперед. |
| `REPEAT` | `REPEAT(str, n)` | Повторяет строку `n` раз. |

**Примеры**

```sql
SELECT REPLACE('beegEek', 'e', 'i');
-- biigEik

SELECT REPLACE('Hello World', 'World', 'SQL');
-- Hello SQL

SELECT REVERSE('beegeek');
-- keegeeb

SELECT REPEAT('bee', 3);
-- beebeebee

SELECT REPEAT('-', 5);
-- -----
```

> [!info] Нюанс
> Поведение `REPLACE` по регистру лучше проверять на своей базе: на результат влияет `collation` и контекст сравнения.

---

## 💡 Практические замечания

- `TRIM()` + `LOWER()` или `UPPER()` удобно использовать для очистки и нормализации текста.
- `LOCATE()` + `SUBSTRING()` хорошо работают вместе при разборе строк по позициям.
- `SUBSTRING_INDEX()` особенно удобен для email, доменов, URL и путей.
- `CONCAT_WS()` часто практичнее, чем `CONCAT()`, потому что аккуратно пропускает `NULL`.
- `CHAR_LENGTH()` считает символы, а `LENGTH()` байты.
- Большинство строковых функций при `NULL` возвращают `NULL`, но исключения и нюансы лучше помнить отдельно.

## 📎 По PDF

> [!note] Статус сравнения
> Я попытался сравнить памятку с файлом `SQL раздел текстовые функции.pdf`, но локально не удалось надежно извлечь из него текст: встроенный браузер блокирует открытие `file://` PDF, а из консоли PDF читается только как бинарный поток.
>
> Поэтому я не буду выдумывать, будто точно сравнил весь состав функций. Сейчас вывод такой: памятка уже покрывает базовый и очень хороший набор текстовых функций MySQL, но утверждать, что в PDF точно нет дополнительных примеров или функций, без нормального чтения PDF нельзя.
