Если говорить кратко, то итератор — это механизм поэлементного обхода данных, а генератор позволяет отложено создавать результат при итерации. 

В частном случае генератор вообще является подтипом итератора. Но не наоборот. Приведенный ниже код: 

```python
import typing

_gen = (i for i in range(5))  # генератор
_iter = iter(range(5))  # итератор

print(_gen)
print(_iter)

print(issubclass(_gen.__class__, typing.Iterator))
print(issubclass(_iter.__class__, typing.Generator))
```

Выводит на экран:

```python
<generator object <genexpr> at 0x7fd579005e00>
<range_iterator object at 0x7fd58f033030>
True
False
```

Интересная статья по этому поводу на хабре: [https://habr.com/ru/post/337314/](https://habr.com/ru/post/337314/)



**@Валерий_Светкин**, За статью спасибо, на досуге почитаю, если еще не читал.

А вот по сабжу: я не совсем понял место

> генератор позволяет отложено создавать результат при итерации

А разве итератор не позволяет это все делать? :D

Чисто по пунктам:

```python
from itertools import tee

gen_ = (i for i in range(5))
iter_ = iter(range(5))

iter1, iter2 = tee(gen_)    # Без ошибок

print(gen_ == iter(gen_))   # True

print(next(gen_))   # Без ошибок
```

1. Генератор также истощается, как и итератор.

2. У генератор также есть и next(), и iter().

3. iter() также вернет себя же.

Поэтому самое точное определение генератору, что я могу дать на данный момент: "Генератор - это удобный способ создания итераторов".

Во всем остальном - это те же итераторы.

P.S. Почему _iter и _gen? Это же, кажись, синтаксис защищенных атрибутов/методов. Вроде как подчеркивание нужно в конце ставить, если не нужно использовать зарезервированные слова. Или нет?